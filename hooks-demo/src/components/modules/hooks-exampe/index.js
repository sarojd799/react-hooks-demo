import useStateExamplefrom from "./use-state";
import React from 'react';

function HooksModule(props) {
    return (
        <div>
            parent
        </div>
    );
}

export default HooksModule;